image: node:latest

cache:
  paths:
    - node_modules/

stages:
  - deploy

test:
  stage: test
  before_script:
    - yarn install
  script:
    - yarn test:unit
  only:
      - master

pages:
  stage: deploy
  before_script:
    - yarn install
  script:
    - yarn build
    - mv public public-vue
    - mv dist public
  artifacts:
    paths:
      - public
  only:
      - master
